/**
 * For slides that consist of one or more headings followed by two or more paragraphs, we replace
 * the paragraphs with a bulleted list with a list item for each paragraph.
 *
 * @param nodes
 * @returns {*[]}
 */
export const summarise = (nodes) => nodes;
