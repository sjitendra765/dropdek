// Clusters
export const clustersPanelLandscapeOverride = (palette) => ({
  '& .sequence .cluster': {
    border: '0.05em solid',
    borderColor: `${palette.text()}44 !important`,
    background: palette.backgroundColor,
    borderRadius: '0',
    boxShadow: 'none',
  },
});
